# Exercise Data

This folder is used for local datasets required by some exercise notebooks.

The raw CSV files are not versioned in the GitHub repository.

Expected local files:

- `CC_GENERAL.csv` for `exercise_04_credit_card_customer_clustering_solution.ipynb`
- `aapl_5y.csv` or `aapl.csv` for `exercise_05_financial_time_series_solution.ipynb`

The dimensionality reduction notebook uses the UCI HAR dataset, which should be placed separately in:

- `exercises/UCI HAR Dataset/`
