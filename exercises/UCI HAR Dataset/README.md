# UCI HAR Dataset Placeholder

The dimensionality reduction notebook expects the extracted UCI Human Activity Recognition dataset in this folder.

Expected structure:

```text
UCI HAR Dataset/
  train/
  test/
  features.txt
  activity_labels.txt
```

The full raw dataset is not versioned here to avoid making the repository unnecessarily heavy.
